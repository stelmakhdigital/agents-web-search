# The @deepseek-ai/* and @earendil-works/* peer dependencies are provided by
# the host deployments (DSH checkout / Pi runtime) and are NOT in the npm
# registry — never auto-install them. Typechecking uses local stubs
# (packages/*/types/) or the host sources (typecheck:host).
auto-install-peers=false
strict-peer-dependencies=false
