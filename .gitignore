node_modules/
lib/
coverage/
*.tsbuildinfo
.npm-cache/
.pnpm-store/
# .vendor/: gitignored EXCEPT the pinned core tarball — it ships with the
# repo so a fresh clone is self-contained (git-channel install, 2026-09-21).
.vendor/*
!.vendor/agents-web-search-core-1.0.0.tgz
# packages/dsh/lib: the esbuild bundle is COMMITTED — it is the DSH plugin
# entry (root package main) for the git-channel install (fresh clone).
# Rebuild + re-commit whenever packages/dsh/src changes (pnpm -r run build).
!packages/dsh/lib/
!packages/dsh/lib/index.js
dist/
# Generated by `pnpm typecheck:host` (recreated from the DSH checkout):
packages/*/tsconfig.host.json
packages/*/types/host-stubs.d.ts
