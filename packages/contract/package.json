{
  "name": "@agents-web-search/contract",
  "version": "1.0.1",
  "description": "Internal host-contract test suite (ADR-002): shared assertions for every HostAdapter implementation (DSH, Pi, future agents). Not published.",
  "license": "MIT",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@agents-web-search/core": "file:../../.vendor/agents-web-search-core-1.0.0.tgz"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.8"
  }
}
